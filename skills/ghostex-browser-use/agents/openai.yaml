interface:
  display_name: "Ghostex Browser Use"
  short_description: "Automate browser pages through Cua Driver"
  default_prompt: "Use $ghostex-browser-use to inspect and operate a supported browser page through Cua Driver."

policy:
  allow_implicit_invocation: true
