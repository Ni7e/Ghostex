# Next agent prompt — Batch 10 (Phase B long tail: signing/updates, ghostex:// routing, logging, shell)

Copy everything below the line into a fresh agent thread.

---

You are continuing the Ghostex → GPUI parity port. Set up context first, then implement Batch 10.

## Read these before doing anything

1. `docs/2026-07-02/gpui-parity-plan.md` — the verified roadmap. Batches 0 (foundations), 1 (Group 1), 2 (Group 2), 3 (Groups 3+4), 4 (Group 5), 5 (Group 6), 6 (Groups 7+8), 7 (Groups 9+10), 8 (Groups 11+12), and 9 (Groups 13+14 remainder + Group 15) are implemented and building; their status blocks list deliberately deferred follow-ups, so do not "fix" those unless your batch includes them.
2. `docs/2026-07-02/reports/shell.md` + `docs/2026-07-02/reports/g13-14-phaseb.md` — verified gap evidence for your items. NOTE both reports predate Batches 0–9; known-stale points:
   - The daemon bootstrap + handshake (shell §1) EXISTS (Batch 0.5: `start_gpui_local_gxserver_bootstrap`, spawn + protocol/tool checks + status toasts). Toasts, the hotkey table, `updateSettingsPatch` (Batch 0), the CLI bridge server on 58743 (Batch 3.4 — the B.10 "native CLI bridge NOT served" gap is CLOSED; only `openFloatingEditor` needs it post-cutover, already audited), session-restore eagerness (Batch 2.9, focused-session slice), and the portless setup prompt + first-run onboarding (Batch 9) all exist now.
   - Every `main.rs` line anchor in both reports has drifted badly (the file is ~2.9MB and Batches 4–9 added thousands of lines) — always `rg` for the named symbols instead of using `:NNNNN` anchors.
   - Batch 9 resolved: `keepAwakeDeactivateOnUserSwitch` = N/A-by-parity (dead setting on shipped macOS); lid-sleep XPC requirement = verified consistent, with the exact Group 16 signing requirements recorded in the plan's §14 status block — READ IT before touching signing; `discoverGhostex` = N/A-by-parity (no sender on either platform).
3. `docs/2026-07-02/deferred-out-of-scope.md` — the running list of consciously deferred items. Append to it whenever you defer something; never silently drop scope.

## Context you must not rediscover

- The app being ported lives in `gpui/` (Rust GPUI shell + CEF-hosted React surfaces). The hybrid architecture is final — no UI rewrites; parity = behavior completeness. GPUI reuses the SAME shared React sidebar + modal host + titlebar host via CEF; the shared titlebar already renders the full update-state UI (`native/sidebar/titlebar-host.tsx` ~:342/:436 `TitlebarUpdateProgressRing`, `updateAvailable/Downloading/Progress`, `showUpdateDialogFromTitlebar`) — your Group 16 item backs it, never re-renders it.
- Reusable patterns (freshest examples): Rust→runtime script-injection bridges with pending queues (`gpui_project_board_conversation_request_script` in main.rs + its `installGpuiBridgeCallbacks` install in `gpui/sidebar/gxserver-runtime.ts`); runtime→Rust manifest `post*` fns (`gpui/src/cef/sidebar_bridge_manifest.rs` + `cef/macos.rs` + `cef/unsupported.rs` + a `receive_*` arm — `postProjectBoardConversationResponse` is the model); app-modal commands land in `handle_gpui_app_modal_sidebar_command` (main.rs; unmatched commands silently drop — check your message has an arm); GPUI state files persist under `ghostex_home_root().join("state/gpui-*.json")` (rg `gpui_first_run_onboarding_state_path` for the freshest example); startup tasks hook into the `view.update` block in `fn main` (rg `start_gpui_first_run_onboarding`).
- Already working (do not rebuild): the daemon bootstrap incl. never-stop-on-quit; the CLI bridge server (58743 + token, `gpui/src/cli_bridge.rs`); LS-registration/status reads for `ghostex://` + editor/terminal targets (rg `LSCopyDefaultHandlerForURLScheme` in main.rs — GPUI can REGISTER as handler; it just handles nothing); the GPUI Info.plist already declares the `ghostex` URL scheme (`gpui/scripts/build-macos-app.sh`, rg `CFBundleURLSchemes`); the macOS Sparkle reference implementation (`AppDelegate.swift` ~:1023-1046, 15-min probes ~:470; `native/macos/ghostexHost/AppInfo.plist:80` SUFeedURL); the existing release pipeline (`scripts/release-ghostex.mjs`, repo-root `appcast.xml` + `appcast-x86_64.xml`).
- Batch 9's lid-sleep signing verify (plan §14 status block) is your signing spec: DevID-sign inside-out (CEF helpers, frameworks, the staged lid-sleep helper, then the app — drop `--deep` at `build-macos-app.sh:590`), hardened runtime on app AND helper for notarization, and expect exactly one lid-sleep helper reinstall prompt on the ad-hoc→DevID transition. No lid-sleep client/helper code changes.
- Browser profiles are OUT OF SCOPE (Decision #8) — GPUI has NO persistent browser storage (CEF `cache_path` deliberately empty). This changes the quit-behavior item: macOS's pre-quit CEF cookie flush (AD:1218-1242) protects browser logins that GPUI deliberately does not persist — verify whether any GPUI-persisted CEF state needs a flush at all, and if not record N/A-by-decision instead of porting it.
- IGNORE the stale trackers `gpui/SETTINGS_PARITY_PROGRESS.md`, `gpui/TITLEBAR_APP_MODAL_PARITY_PROGRESS.md`, `gpui/WORKSPACE_PARITY_PROGRESS.md`, and `docs/gpui-parity.md`.
- `gpui/src/main.rs` (~2.9MB), `gpui/sidebar/gxserver-runtime.ts` (~550KB), and `native/sidebar/native-sidebar.tsx` (~47k lines) — rg for symbols then read targeted ranges; never read whole files.

## Hard rules

- No CDXC comments. No tests in `gpui/` and none in the macOS app (repo policy — AGENTS.md; `shared/` and `gxserver-rs` tests are allowed — note this constrains the Group 18 "writer-boundary tests" idea: log writers living in `gpui/src` get no tests; put any testable pure logic in `shared/` only if it genuinely belongs there).
- Logging code becomes IN scope for this batch ONLY inside item 3 (Group 18 is the logging item); do not sprinkle logs anywhere else.
- Never run `bun run start` or anything that restarts a running app unless the user asks.
- Verify with `cargo check` in `gpui/`; sidebar bundles build with `bunx vite build --config gpui/vite.config.ts` from the repo ROOT; if you touch `shared/`, `sidebar/`, or `native/sidebar/`, also run `bun run typecheck` (repo root). `gpui/sidebar` is type-checked by neither; a targeted tsc there surfaces the 11 pre-existing branded-type errors in untouched code — do not add new ones.
- Work one compile-clean commit-sized chunk at a time; after each item, ask the user to run the app and verify side-by-side with the macOS app before moving on.
- Follow AGENTS.md search routing (exclude `ghostty/**`, vendor trees, `node_modules/**`, `target/**`, builds).
- Never add fallbacks where correct behavior is possible; no hitTest/overlay hacks. Native OS-owned menus and native child windows ARE the accepted patterns for overlay surfaces. Honest stub toasts are the accepted pattern for affordances blocked on an open decision.

## Decisions already made (do not re-ask)

- Browser profiles (Decision #8), desktop pet floating window (Decision #9): OUT OF SCOPE. Phone notifications (Decision #10): N/A. Pop-out panes (#2), restore eagerness (#3), zehn text search (#4), App Icon picker (#5): resolved and landed.
- T3 runtime ownership (Decision #7): still OPEN by user choice, an explicit FUTURE batch — if the user resolves it, that batch REPLACES this one and this prompt carries to Batch 11.
- git-editor + automate surfaces (Decision #6): still OPEN — ask only if the user brings it up.
- Group 16 pull-forward into Batch 9 was DECLINED (user 2026-07-03) — signing/updates lead THIS batch by plan order.

## Your job: Batch 10 — Phase B long tail (Group 16 → 17 → 18 → Group 0 shell)

Work these in order, one compile-clean commit-sized chunk each. Line anchors drift — rg the symbols.

1. **Group 16: Sparkle + signing + release (L — plan §16).** ASK THE USER FIRST for their Developer ID identity/team + notarization credential setup (keychain profile vs env vars) — the build must be parameterized, not hardcoded. Then: (a) integrate Sparkle into the GPUI bundle (framework staging in `build-macos-app.sh`, SUFeedURL/SUPublicEDKey in the app plist — macOS reference `AppInfo.plist:80`, AD ~:1023-1046 for the controller + ~:470 for the 15-min probe cadence) with a Rust-driven user driver: back `checkForUpdate`/`downloadUpdate`/`showUpdateDialogFromTitlebar` in main.rs (they currently have NO arms — the shared titlebar renders everything) and feed `updateDownloadProgress` to the titlebar host. (b) Replace ad-hoc signing (`build-macos-app.sh:590`) with the inside-out DevID + hardened-runtime + notarize + staple flow speced in plan §14's Batch 9.3 notes; make signing optional/parameterized so unsigned dev builds keep working. (c) Real versioning (CFBundleShortVersionString is hardcoded 0.1.0 in the build script) + extend `scripts/release-ghostex.mjs` and the appcast for the GPUI bundle id (`com.madda.ghostex.gpui`). (d) Close the G13 🔎: the build validates prebuilt Linux remote-gxserver packages exist (`build-macos-app.sh` rg `remote-gxserver-linux`) — make the release pipeline produce/stage both or document the manual step.
2. **Group 17 remainder: runtime `ghostex://` + file-open routing (M — plan §17 + Group 0; shell §9).** AppKit open-urls/open-files delegate via a `.m` shim (the `Gpui*.m` shims in `gpui/native/macos/` + `build.rs` are the pattern): `ghostex://terminal?command&cwd&title` → quick terminal, `ghostex://open|edit?path&line&column` → open paths, Finder Open-With file opens, and the `.command/.tool/.sh` Run/Edit/Cancel dialog (macOS `handleOSIntegrationURL` AD:4798, `dispatchOSIntegrationFileOpenPaths` AD:4839, dialog AD:4859). Route into existing GPUI session-creation/open paths — no new UI. Then the CLI activation fix (S): `ghostex-cli.mjs` ~:3890 activates `"Ghostex"` by name; parameterize to bundle-id activation so the GPUI app (`CFBundleName "Ghostex GPUI"`) can be activated — this touches the shared CLI, so keep macOS behavior identical.
3. **Group 18: logging + crash reporting (M — plan §18 + Group 0; shell §10).** `std::panic::set_hook` → crash reports under `~/.ghostex/logs/`; sanitized, size-capped writers for the GPUI flows macOS logs (remote install, sidebar refresh, terminal focus — see the `*DebugLog.swift` writers), with macOS-convention retention (AD:1115). This also un-defers the Batch 8.2 board `diagnosticLogging` and Batch 8.3 generateTitle diagnostics notes — sweep `deferred-out-of-scope.md` for entries gated on "the Group 0 logging item" and close the ones this enables. Then the 🔎: confirm nothing consumes the vestigial `GHOSTEXHomeDirectoryName=.ghostex-gpui` Info.plist keys (`build-macos-app.sh`; Rust uses `GHOSTEX_HOME` else `~/.ghostex` — `shared_settings.rs` rg `ghostex_home_root`) and remove or fix them.
4. **Group 0 shell items (M total; shell §2/§4/§5).** (a) Native app menu bar via `cx.set_menus`: App (About/Check for Updates — wire to item 1/Settings/Hide/Quit), File→Close Pane ⌘W, Edit clipboard set, Window→Minimize/Zoom (macOS `installMainMenu` AD:2533-2663). (b) Window frame persistence: bounds + screen id with multi-monitor restore logic (AD:3205-3260); GPUI currently always opens centered 1280×820 (rg `WindowBounds::centered` in `fn main`). Persist under the `state/gpui-*` pattern. (c) Quit behavior: will-terminate persistence flush, dock-reopen or quit-on-last-window-close (macOS quits on last window close — AD `ShouldTerminateAfterLastWindowClosed`), and resolve the pre-quit CEF flush question per the Decision #8 note above.
5. **Runtime checks (with the user).** Signed build installs + updates from the appcast on a second machine (or at minimum notarizes + staples clean); titlebar update ring animates during a download; `ghostex://terminal|open|edit` and Finder Open-With reach the running app; a forced panic writes a crash report; the app menu bar works incl. ⌘W close-pane; window frame survives relaunch on a moved/second display; quit flushes state and last-window-close quits. Side-by-side with the macOS app.

## Decisions needed from the user

- Item 1 signing/notarization credentials + how to parameterize them (genuine user input — ask before building).
- Decisions #6 (git-editor/automate surfaces) and #7 (T3 runtime ownership) stay open; do not resolve them inside Batch 10. If the user resolves #7, the T3 runtime ownership batch takes precedence and this prompt becomes Batch 11.

## When you finish Batch 10

1. Update `docs/2026-07-02/gpui-parity-plan.md`: add a Batch 10 status block under Groups 16, 17, 18 and Group 0 with items done (one-line notes on anything deferred), same style as the Batch 0–9 status blocks, and update the "Suggested execution order" line. Append deferred items to `docs/2026-07-02/deferred-out-of-scope.md`.
2. Write `docs/2026-07-02/next-agent-prompt.md` (overwrite it) with the exact prompt for the NEXT agent thread to implement Batch 11 — scope it from what actually remains: the T3 runtime ownership batch if Decision #7 is resolved (or re-present the decision if not), any Batch 10 items that slipped, the remaining small Group 0 🔎 items (pane-divider min-clamps + double-click reset, workspace bg from Ghostty config, default sidebar width, full Resources dropdown, editable titlebar session title), and a final sweep of `docs/2026-07-02/deferred-out-of-scope.md` for entries whose blockers have landed. Anchor it on the plan + `docs/2026-07-02/reports/` evidence. That prompt must have this same structure: read-first list, context (including what must not be rebuilt), hard rules, ordered work items with file anchors from `docs/2026-07-02/reports/`, decisions needed, and — verbatim requirement — an instruction that the Batch 11 agent must in turn write the Batch 12 prompt the same way.
3. End your final message by pasting the full Batch 11 prompt inline so the user can copy it.
