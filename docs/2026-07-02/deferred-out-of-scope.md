# GPUI Parity — Deferred / Out-of-Scope Tracker

Living document. Every parity batch appends items here that were consciously deferred or declared out of scope, so we can sweep them at the end instead of losing them. Keep entries short: what, why deferred, where the code hooks are, and what "done" would look like.

Format: `- [origin batch] item — reason — re-entry pointer`

## Deferred by user decision

- [Batch 2 / G2, Decision #2 2026-07-02] **Pop-out pane windows** — user: keep no-op for now, revisit later. macOS re-parents the surface into a real NSWindow (`native/macos/.../TerminalWorkspaceView` ~:16356-16553). GPUI `popOutPane` message stays a silent no-op. Done = a real GPUI window hosting the re-parented terminal surface with return-to-layout.

## Deferred from Batch 0 (see plan's Batch 0 status block for detail)

- [Batch 0.2] Rust-originated in-modal toasts still render inside the open modal window (remote-clone flow depends on it); toast action buttons unsupported.
- [Batch 0.3] ~~SEARCH / MOUSE_OVER_LINK Ghostty action tags — land with Batch 2 UI.~~ Landed in Batch 2 (2026-07-02). DESKTOP_NOTIFICATION stays undispatched on purpose: verified the macOS host's `handleAction` does not handle it either, so parity = no-op. Revisit only if macOS adds it.
- [Batch 0.4] Removed/remapped-away chords stay bound until relaunch; hardcoded base binds still registered alongside remaps.
- [Batch 0.5] Daemon bootstrap does not gate window creation; buildIdentity check + auto-restart on toolchainUnavailable replaced by warning toast; stop-control-plane API not ported.

## Deferred from Batch 1

- [1.2] **Remote-session Full reload / Close After Done parity edges** — GPUI `fullReloadSession`/`restartSession` handle local sessions only (macOS also reloads remote presentation sessions via `reloadRemotePresentationSession`). Close After Done timers do track remote rows, but firing closes via the standard remote `killSession` path without the macOS local lifecycle pre-patch. Re-entry: `gpui/sidebar/gxserver-runtime.ts` `fullReloadSession` / `completeCloseAfterDoneTimer`.
- [1.2] **sleepInactiveProjectSessions delayed-send exclusion** — macOS additionally excludes sessions with an active Delayed Send from bulk sleep; GPUI's runtime does not have an Agents-session delayed-send projection to check yet (command panes track it in Rust). Re-entry: `collectInactiveProjectSessionIds` once a delayed-send projection exists.
- [1.4] **Search-by-Text atuin/history prefix** — macOS launches `gx f` with `withAtuinIgnoredShellHistoryPrefix` so it stays out of shell history; GPUI launches it as an agent-session launch command without the prefix. Cosmetic difference.
- [1.5] **Named groups for remote-machine projects** — the new client-side group model covers local projects only; remote-machine project sections do not offer New Group. Re-entry: extend `workspace-session-groups.ts` ids to carry a machine scope.
- [1.5] **Group model is GPUI-local, not the shared macOS module** — GPUI persists a session-id→group overlay (`gpui/sidebar/workspace-session-groups.ts`) rather than adopting `shared/simple-grouped-session-workspace-state.ts` wholesale, because GPUI's sidebar derives groups from gxserver presentation snapshots instead of owning a full client-side session-grid workspace. Semantics (ids `group-${n}`, max 20, create-from-session, order sync) match. Note: shipped macOS currently exposes NO group-creation UI (the handlers exist but nothing posts `createGroup`); GPUI now exposes "New Group" / "Move to New Group" gated on the new `canCreateSessionGroup` contract flag, so macOS rendering is unchanged.

## Deferred from Batch 2

- [2.1] **OSC titles are render-local; no gxserver title sync** — macOS forwards visible terminal titles into gxserver (`syncNativeSessionActivityWithGxserver` / `syncSessionTitleFromNativeTerminalTitle` in `native/sidebar/native-sidebar.tsx`) so card titles, agent detection inputs, and persisted titles update; GPUI only overlays workspace/command tab labels at render time. Re-entry: forward SET_TITLE through a Rust→runtime bridge (bell pattern) and reuse the shared title-sync rules.
- [2.1] **Rust title-trust rules are a port, not the shared module** — `gpui/src/terminal_osc_title.rs` mirrors `getVisibleTerminalTitle` from `shared/session-grid-contract-session.ts`; keep the two in sync when the shared patterns/sets change.
- [2.2] **Bell forwarding scope** — command panes (no gxserver identity) and remote attach terminals do not forward BEL into attention; macOS's transient activity-suppression window (`getNativeActivitySuppressedUntil`) is not ported. Re-entry: `dispatch_gpui_workspace_terminal_bell` (main.rs) + `handleGpuiWorkspaceTerminalBell` (gxserver-runtime.ts).
- [2.4] **Search-bar Escape/Up/Down under GPUI key dispatch** — those keys are handled on the bar's key-down listener while the gpui-component input owns focus; if GPUI's Input-context actions consume them, Return/Shift+Return and the prev/next/close buttons remain the guaranteed paths. Verify at runtime. (macOS Cmd+G inside a terminal deliberately goes to the child TUI, not search navigation — GPUI matches by not binding it.)
- [2.5] **Fresh-install hotkeys map** — Rust binds only the persisted settings `hotkeys` map (Batch 0.4 design: no default table mirrored in Rust); a machine that never saved settings gets no configured chords until the first settings save. Re-entry: seed normalized hotkeys at daemon bootstrap or mirror `DEFAULT_ghostex_HOTKEYS`.
- [2.6] **Remote fork/reload from native surfaces** — the new `onWorkspaceTerminalRuntimeAction` bridge resolves local workspace mappings only; remote attach terminals get no tab-menu/hotkey fork/reload (sidebar card actions already cover remote). Re-entry: extend `dispatch_gpui_workspace_terminal_runtime_action` with the remote-attach key map.
- [2.7] **Agents Delayed Send: no persistence, no tab chrome** — Agents timers are runtime-only (command-pane timers persist + restore across relaunch) and Agents tabs show no countdown badge/tooltip. Re-entry: `agents_delayed_send_timers` (main.rs) + the command-pane badge/persistence patterns.
- [2.9] **Auto-materialize is focused-session-only** — Rust's workspace attach guard (`receive_sidebar_workspace_terminal_focus_payload`) admits only the currently focused session, so other previously-visible split sessions stay lazy placeholders; the re-attached session also gets a fresh tab rather than adopting its restored placeholder tab (same behavior as clicking the card after relaunch). Re-entry: a startup attach batch that bypasses the focused-session guard + restored-placeholder tab adoption in `open_gpui_local_workspace_terminal`.

## Deferred from Batch 3

- [3.1] **First-prompt Enter-submit skips background tabs** — GPUI can press Return only on the mounted active tab of a pane; if the user switches tabs while the title is generating, the staged `/rename` stays unsubmitted in the prompt (macOS sends Enter to hidden views). Re-entry: `send_enter_key_to_local_agents_workspace_session` (main.rs) once background-tab key delivery exists.
- [3.1] **No terminal-input-suppression "Generating title" overlay** — macOS Swift draws a blocking pane overlay and suppresses typing during generation; GPUI only holds the sidebar card spinner until after the Enter submits. Re-entry: Agents terminal body chrome (same normal-layout row discipline as the search bar).
- [3.5] **No prompt-editor prewarm** — macOS prewarms a hidden Monaco child window after launch (`prewarmFloatingPromptEditorIfNeeded`); GPUI cold-opens, so the first Ctrl+G pays CEF+Monaco startup. Re-entry: prewarm the reusable app-modal host with the prewarm open message contract already present in `modal-host.tsx`.
- [3.5] **Editor window frame is fixed 432×352** — macOS persists a draggable/resizable native frame (`floatingPromptEditor.frame.v1` defaults key) and passes `initialFrame` to React; GPUI opens a fixed-size centered window, and React's in-page move/resize moves the panel inside the window instead of moving the window. Re-entry: GPUI window-frame persistence + `initialFrame` in the open message.
- [3.5] **Native red-X close of the editor window skips the status handshake** — no GPUI app-modal window has a native-close observer (pre-existing gap for all modals); a red-X close leaves the CLI polling until Save/Cancel/replacement or app quit (socket close ⇒ CLI resolves saved). Save/Cancel/Ctrl+G/Escape are the supported paths. Re-entry: window-close observation on the app-modal handle + `saveActiveFloatingPromptEditorForAppLifecycleClose` parity on quit.
- [3.5] **Closing the originating terminal does not close-and-save the editor** — macOS sends `floatingPromptEditorCloseAndSave` when the launching terminal closes. Re-entry: the local workspace close path in main.rs.
- [3.6] **TIFF/HEIC/PNM previews (and clipboard TIFF bitmaps) are not re-encoded** — macOS re-encodes everything to PNG via NSImage; GPUI has no native image decoder, so previews are limited to Chromium-renderable formats (png/jpg/gif/webp/svg/bmp/ico/avif, 10MB cap) and a TIFF bitmap paste stores `.tif` without a thumbnail. Re-entry: add an image decode dependency or a CoreGraphics FFI re-encode.
- [3.4] **58743 port contention between the two apps** — whichever Ghostex app binds first owns the CLI bridge and the token file (last writer wins); the second app shows a warning toast and runs bridge-less, so Ctrl+G in its terminals uses the machine editor. Same single-owner contract as macOS; matters for side-by-side parity testing.

## Deferred from Batch 4

- [4.1] **Git menu presentation deltas vs the macOS dropdown** — the OS-owned NativeMenu has no Status/Actions section labels, no icons or colored stat pairs, no branch tooltip (branch copy is a plain menu row), and no busy spinner. Functional rows and disabled gating match. Re-entry: `show_gpui_titlebar_git_menu` (main.rs) if a richer CEF titlebar-host panel is ever wanted.
- [4.1] **First-open placeholder** — clicking the git glyph before the runtime's first state post shows a single disabled "Loading Git state..." row (a background refresh is dispatched on open). Self-heals within one publish.
- [4.4] **Periodic full Git state covers the active LOCAL project only** — remote-project active groups refresh full state on menu open and before every action run (both re-read state), not on the 15s cycle, to avoid remote toast spam and per-cycle remote load. Remote diff stats DO refresh on the cycle. Re-entry: `refreshProjectDiffStatsTarget` (gxserver-runtime.ts).
- [4.4] **Titlebar indicator is the badge dot only** — macOS also swaps the git button glyph to the resolved primary-action icon and shows a spinner while busy; GPUI keeps the static git glyph + the existing badge-dot pattern. Re-entry: `render_titlebar_icon_button` `"git"` (main.rs).
- [4.5] **Attention-triggered diff-stats refresh not ported** — macOS refreshes a project's stats when one of its sessions enters attention (`refreshProjectDiffStatsForAttentionSession`); GPUI relies on the 15s staggered cycle. Re-entry: attention transitions in the runtime presentation delta path.
- [4.5] **Remote closed-to-recent exclusion not mirrored** — macOS skips remote projects closed to Recents (`isRemoteProjectClosedToRecent`); GPUI probes every remote presentation project because its remote-recents model differs. Cosmetic extra probes only.
- [4.6] **Standalone GitFileDiff modal stays trigger-less by parity** — the macOS titlebar git dropdown has no changed-files list and its own standalone trigger is dead; diff-within-commit covers per-file diffs. Re-entry: add a changed-files submenu to the git menu and route rows to the registered `gitFileDiff` kind.

## Deferred by user decision (continued)

- [Batch 5 / G6, Decision #5 2026-07-02] **App Icon picker: hidden on GPUI** — user chose hide over porting the macOS subsystem (~/.ghostex/icons PNG library, ≤2048px/≤5MB validation, squircle-masked thumbnails, Dock apply, NSOpenPanel pick). GPUI's app-modal hydrate sets `hud.appIconPickerUnavailable: true`; the shared SettingsModal hides the App Icon section and skips the `listAppIcons` request (macOS never sets the flag, so its modal is unchanged). Done = Rust arms for listAppIcons/setAppIcon/pickAppIconFile + Dock apply via NSApplication + thumbnail generation (needs an image decode path — same constraint that deferred TIFF re-encode in Batch 3.6), then drop the flag from the hydrate.

## Deferred from Batch 5

- [5.2] **Open-target manual re-scan has no trigger (parity)** — the macOS host protocol declares `refreshWorkspaceOpenTargetAvailabilityFromTitlebar` but nothing in shipped macOS sends it (titlebar Open In dropdown = target rows + Configure only), so the live macOS behavior is the startup scan alone. GPUI ports exactly that: `start_gpui_workspace_open_target_availability_scan` at launch. Re-entry: a "Refresh Targets" row in `show_gpui_open_targets_menu` (main.rs) re-running the scan with force-persist semantics if a manual recheck is ever wanted.
- [5.6] **git-editor auto-sleep family stays out of scope** — `autoSleepGitEditor*` settings keys exist and macOS enforces them (`native-sidebar.tsx` ~:43004), but GPUI has no git-editor surface at all; the whole family is tied to Decision #6 (Group 10 git-editor + automate surfaces). Re-entry: lands with the git-editor surface decision; wire a GitEditor variant into the Rust `TitlebarMode` auto-sleep mapping (main.rs ~:67333) plus the duration keys in shared_settings.rs.
- [5.6] **Require-resume exclusion checks daemon-published fields, not per-agent catalog validation** — GPUI's `autoSleepRequireAgentResumeCommand` gate (new in Batch 5) excludes sessions with no `agentSessionId`/`agentSessionPath`/`trustedResumeTitle` in the gxserver presentation. macOS additionally validates per-agent reference formats and that the agents catalog has a launch command (`canRestoreNativeTerminalSession`, native-sidebar.tsx:22554). The daemon normalizes those fields before publishing, so the practical delta is small; a session with a stale-but-present reference could still auto-sleep on GPUI where macOS would keep it awake. Re-entry: expose a daemon-computed `canResumeWithAgent` on the presentation session (gxserver-rs presentation.rs) and gate on that instead.
- [5.4] **Ghostty terminal settings: no live reload; config loads at Ghostty-app-owner creation only** — code-verified 2026-07-02. Saves DO write the managed Ghostty config file (`sync_gpui_ghostty_config_file_after_settings_save` → the same `~/Library/Application Support/{com.mitchellh.ghostty,com.ghostty.org,Ghostty}/config[.ghostty]` candidates macOS writes), and `font-size` reaches every new surface immediately via the per-surface config request maps refreshed in the post-save fan-out. But theme/font-family and the other config-backed keys are read only by `ghostty_config_load_default_files` at `GhosttyAppOwner` creation, and GPUI's two app owners (`agents_terminal_ghostty_app`, `command_terminal_ghostty_app` in main.rs) are created lazily once per app run and never recreated — so those edits reach terminals at relaunch (or the family's first-ever terminal in the current run if none existed yet). Running surfaces never reload: the GPUI GhosttyKit FFI has no `ghostty_app_update_config` binding. macOS by contrast live-reloads all surfaces on a 3s debounce (`scheduleGhosttyConfigReload` → `Ghostty.reloadConfig()` → `ghostty_app_update_config`, AppDelegate.swift:5486 / TerminalWorkspaceView.swift:1785). Re-entry: bind `ghostty_app_update_config` in `ghostty_kit.rs`, keep a `GhosttyConfigOwner` reload path, and call it from the fan-out with the same debounce semantics. Do not fake it by dropping/recreating surfaces.

## Deferred by user decision (Batch 7 scope, 2026-07-02)

- [Batch 7 / G9, Decision #8 2026-07-02] **Browser profiles: entirely out of scope** — user: "no need for browser profiles". This covers BOTH the small persistence fix (per-profile CEF `cache_path` stays deliberately empty, `gpui/src/cef/macos.rs` `cef_request_context_for_profile` ~:2298-2346 — cookies/logins die with the app) AND the larger named-profiles + Import Browser Data port (~940 lines Swift incl. Keychain Safe-Storage decrypt; macOS `GhostexCEFBridge.mm:797-835`, `NativeBrowserProfiles.swift`). Done = persistent per-profile on-disk `cache_path` under a GPUI-owned root (e.g. `~/.ghostex`), then optionally names/import on top.
- [Batch 7 / G12, Decision #9 2026-07-02] **Desktop pet floating window: out of scope for now** — GPUI pet stays in-window bottom-right; macOS is a draggable, all-Spaces floating panel with activity bubbles + persisted position. The animation engine is already ported — done = a windowing/presentation port (floating GPUI window, drag + position persistence, activity bubbles).

## Deferred from Batch 7

- [7.2] **Renderer browser-open response carries no `reused`/`session` payload** — macOS returns `{ok, reused, session}` to the CLI; GPUI's `postOpenBrowserUrl` bridge is fire-and-forget, so the runtime answers `{accepted, action, ok}` before Rust resolves reuse. The behavior (navigate-existing vs new tab) matches; only the CLI's printed JSON differs. Re-entry: a result script bridge mirroring `onT3SessionBrowserAccessResult` if the payload is ever needed.
- [7.2] **Browser-open project scoping is accepted-but-ignored** — GPUI's Browser shell is app-global (one tab set per window), so `--project-id/--project-path/--project-name/--groupId` cannot scope reuse or creation the way macOS per-project workspace groups do. Re-entry: per-project Browser workspaces, if GPUI ever grows them.
- [7.4] **GPUI-specific CDP env var wins over the shared name when both are set** — deliberate, so side-by-side runs can split ports (`GHOSTEX_GPUI_CEF_REMOTE_DEBUGGING_PORT` > `GHOSTEX_CEF_REMOTE_DEBUGGING_PORT` > 9334).
- [7.6] **t3BrowserAccess does not cold-start the T3 runtime** — macOS posts `startT3CodeRuntime` and polls the bearer for up to N retries; GPUI (no T3 runtime authority, Decision #7) issues the pairing link against the already-running loopback server and shows an honest error toast when the bearer/server is missing. Re-entry: the Decision #7 batch adds the start+wait path to `gpui_issue_t3_browser_access_link`.
- [7.6] **t3ThreadId modal stays unregistered and trigger-less (parity)** — verified NO `showT3ThreadIdModal` sender exists anywhere on either platform (the shared contract type + modal component ship unused). GPUI deliberately does not register the modal kind so a dead-but-clickable Confirm can never ship. Re-entry when a sender appears: register `GpuiAppModalKind::T3ThreadId` + add a `setT3SessionThreadId` app-modal arm that relinks the gxserver T3 session's providerState thread binding (macOS `relinkNativeT3SessionThread`).
- [7.6] **Open-link allowlist is latest-issued-only** — the Browser Access modal's Open link opens only the exact URLs Rust issued for the most recent request (macOS opens the message URL unvalidated). A stale modal's link stops working after a newer request; reopening Remote Access re-issues.
- [7.7] **Docs host actions not ported** — `openDocsFoldersSettings` (route to Settings modal Projects tab), `revealInFinder` (NSWorkspace select), and `addToSessionContext` (read file → stage fenced prompt into the active agent terminal) return "Unsupported Docs file action." on GPUI. Re-entry: `manage_files_bridge_result` (main.rs) + macOS `handleManageFilesBridgeRequest` (TWV ~:11407-11533) for the contracts.
- [7.7] **No live Docs file-watcher** — macOS watches the project root + docs scan trees (bounded FSEvents set) to refresh the sidebar on external changes; GPUI refreshes only on page-initiated `list` requests. Re-entry: macOS `manageFileWatchDirectories` (TWV ~:12456) for the directory-set rules.
- [7.7] **Docs listing sort + package-descendant deltas** — GPUI sorts by raw byte order (macOS `localizedStandardCompare`) and does not mirror `skipsPackageDescendants`. Cosmetic ordering/inclusion differences only; both sides keep dirs-first and the same caps (1200 entries / depth 8).

## Deferred from Batch 6

- [6.1] **Completion sound rings for local presentation deltas only** — remote presentation sessions entering attention do not play the sound/flash. On macOS, attention audio comes from local native session state (remote-ATTACHED sessions are local terminals there and do ring); unattached remote presentation rows do not ring on macOS either, so the practical delta is remote-attach scenarios. Re-entry: run `detectSessionAttentionCompletionSounds` over the remote presentation delta path in `gxserver-runtime.ts` with remote-shaped session keys.
- [6.1] **Webview sound playback path stays unused in GPUI** — nothing populates `window.__ghostex_SOUND_URLS__`, so SidebarApp's `playCompletionSound` listener contributes only the card flash; audio is Rust-owned (`postSessionCompletionSound` → bundled mp3 + afplay), matching macOS's native `playSound` ownership. If webview audio is ever wanted, stage sound URLs AND remove the native bridge call so completion doesn't double-play.
- [6.3] **killTerminalDaemon sleeps local daemon sessions only** — the Running Sessions modal lists local daemon state, so GPUI's `sleepAllLocalDaemonSessions` skips remote presentations; macOS's `closeAllNativeSessions` flatMaps its full sidebar groups (which can include remote rows) through the shared sleep path. Re-entry: extend `sleepAllLocalDaemonSessions` with the remote presentation walk used by `sleepInactiveSessionsFromTitlebar` if remote scope is ever wanted.
- [6.4] **T3 kill stubs blocked on Decision #7 (now an explicit future batch)** — `killT3RuntimeServer`/`killT3RuntimeSession` stay honest refresh-with-message stubs and GPUI publishes no `t3Server`/`t3Sessions` inventory; verified the shared modal renders the Kill Server button disabled and no T3 session rows, so nothing dead-but-clickable ships. Done = the Decision #7 batch (see plan execution order).
- [6.5] **Monaco worker under file:// CEF unverified** — Batch 6 stages `monaco-editor/min/vs` beside the CEF entries (fixing the total absence of Monaco assets in GPUI), but worker creation from file:// documents may still make Monaco fall back to its main-thread tokenizer mode. Confirm in the runtime pass; if workers fail, editor features still function (slower highlighting only).
- [6.5] **Previous-session restore focus follow-up requires URI-identity ids** — the Rust-built combined focus id is constructed only when project/session ids contain URI-unreserved characters (always true for daemon-generated ids); other ids restore correctly but skip the focus/mount follow-up. Re-entry: `gpui_combined_presentation_session_focus_id` (main.rs) if id formats ever widen.
- [6.6] **Scanner drift test covers catalog paths, not classification logic** — `shared/gpui-agents-hub-scanner-parity.test.ts` asserts both scanners reference the same home-relative roots/files; hooks-vs-configs classification rules, walk depths, and extension filters can still drift silently. Re-entry: extend the source test with extracted classification structure when the scanners next change.

## Deferred from Batch 8

- [8.1] **Automations surface ENTRY gated on Decision #6** — all `automation*` board-bridge actions are live, but GPUI has no surface that opens `tasks-placeholder.html?surface=automations`: macOS enters via the sidebar Automations row / palette (`openAutomationsPage` → Quick "Automations Overview" page) and the titlebar `automate` project-editor mode, both of which ARE the Decision #6 "automate" surface. GPUI's runtime answers `openAutomationsPage` with an honest stub toast (`gxserver-runtime.ts` sidebar-message switch). Done = the Decision #6 surface: a Kanban-slot URL variant carrying `surface=automations`(+`scope=all` for the overview) and the macOS `showBetaFeatures` seed param (`setAutomationsExperimentalGateParam`), plus the row/palette/titlebar entries.
- [8.1] **GetAllState aggregation is raw per-project concat** — macOS re-normalizes merged definitions/runs through `normalizeAutomationDefinitions/Runs` (global ordering); GPUI concatenates the daemon's already-normalized per-project arrays in project order. Cosmetic ordering difference only. Re-entry: `gpui_automation_all_projects_state` (main.rs).
- [8.1] **OpenRunSession resolves the session's project via worktree-path only** — same rule as the daemon's own `find_run_session_project_id` (worktree path → project, else the automation's project). A thread-mode automation whose thread session lives in a DIFFERENT project focuses the wrong scope on both the daemon watcher and GPUI; macOS's in-memory `automationRunBySessionId` map could do better only within one app run. Re-entry: persist `sessionProjectId` on the gxserver run record.
- [8.2] **Board conversation responses ride the 32KB sidebar-bridge post cap** — the same `SIDEBAR_BRIDGE_PAYLOAD_MAX_CHARS` budget every runtime→Rust post shares (incl. the full active-project context). A pathological state (hundreds of links/sessions) would drop the post and surface as the page's 60s bridge timeout. Re-entry: per-function budget in `sidebar_bridge_manifest.rs` if it ever bites.
- [8.2] **getState omits `diagnosticLogging`/board debug logs** — GPUI writes no logs (repo rule: logging lands later), so the page's `native.project.board` debug scenario can never enable and `appendDebugLog` answers as a state echo. Re-entry: the Group 0 logging item.
- [8.2] **Link restorability is a 60s-TTL daemon lookup; restored links keep their stored agent metadata** — macOS resolves restorability from its previous-sessions cache and refreshes link agent fields from the restored native session; GPUI checks `listPreviousSessions` per non-live link behind a TTL cache and keeps the link's existing agent fields on restore (the presentation row for the restored session does not exist yet at write time). Practical delta: stale agent label on a restored link until the next association. Re-entry: `checkGpuiProjectBoardLinkRestorable` / `replaceGpuiProjectBoardConversationLinkSession` (gxserver-runtime.ts).
- [8.2] **startWork focuses through the create path, not macOS's terminalReady-gated pane placement** — macOS defers focus until native Ghostty mounts the pane (`focusProjectBoardStartedSession` after `waitForNativeTerminalReady`, `createFocusedTabGroupPlacement`); GPUI uses the standard create-session focus (same as sidebar plus-button launches). Behavior matches GPUI's own session-creation UX. Re-entry: only if the user pass shows focus landing before the terminal mounts.
- [8.3] **generateTitle runs unbounded and unlogged like macOS** — the prompt-agent subprocess has no Rust-side deadline (macOS `waitUntilExit` has none either; the page's 60s bridge timeout owns UX) and none of macOS's scenario-gated title-generation diagnostics (repo rule: no logging yet). Re-entry: `gpui_project_beads_generate_title` (main.rs) when the logging item lands.

## Deferred from Batch 9

- [9.1] **Portless setup prompt defers while the single app-modal window is occupied** — macOS stacks child windows and opens the prompt over whatever is up; GPUI's one-window modal host skips the auto-open when a modal is already open (without consuming the once-per-run guard) and re-evaluates on the next portless state update. Re-entry: `maybe_open_gpui_portless_setup_prompt` (main.rs) if GPUI ever grows stacked modal windows.
- [9.1] **Prompt evaluation cadence is bootstrap-healthy + portless-update, not per-HUD-publish** — macOS re-runs `resolvePortlessSetupPrompt` on every sidebar publish; GPUI owns portless state in Rust and re-evaluates at daemon-healthy and after every `/api/updatePortlessState`/admin-result round-trip. A prompt condition that becomes true purely daemon-side mid-run (e.g. listeners coming alive with no GPUI-side update) is caught at next launch instead of the next publish. Re-entry: add a check to a future portless health poller if one is ever built.
- [9.1] **Protocol-change auto-reconfigure not ported (pre-existing G6/G13 slice contract)** — macOS `syncPortlessProtocolSetting` automatically runs the `reconfigure` admin action when a ghostex/standalone service is installed; GPUI's settings-save fan-out only syncs gxserver state ("without running privileged Portless admin scripts from this slice", main.rs CDXC) — the user runs the recommended Reconfigure from Settings, or the now-live standaloneReconfigure prompt surfaces it. Re-entry: `sync_gpui_portless_settings_after_save` (main.rs).
- [9.2] **keepAwakeDeactivateOnUserSwitch stays a no-op BY PARITY** — verified dead on shipped macOS (toggle persists and is forwarded to titlebar-host.tsx but never read; no user-switch observer exists in the Swift host). Re-entry: port the concrete behavior if macOS ever implements one (`NSWorkspace.sessionDidResignActiveNotification` → deactivate keep-awake runtime).
- [9.3] **Ad-hoc signing pins the lid-sleep helper's client requirement to each build's cdhash** — every GPUI rebuild costs one admin reinstall prompt at the next lid-sleep enable (self-healing via the first-enable install gate); heartbeats from a rebuilt app fail until then. Fix ships with Group 16 DevID signing (inside-out signing, hardened runtime on app + staged helper; capture-at-install then yields a stable certificate-anchored requirement — no client/helper code changes). Re-entry: Group 16 build-script work (`build-macos-app.sh:590`).
- [9.4] **discoverGhostex stays trigger-less by parity** — no sender exists on either platform (macOS auto chain superseded by the tutorial video 2026-06-18; `showFirstLaunchSetupOnClose` machinery is dead code). The GPUI modal kind stays registered like macOS's registry entry. Re-entry: wire a GPUI trigger (Tips row / menu) when macOS grows one.
- [9.4] **First-run revision constants are mirrored in Rust, not imported** — `GPUI_FIRST_LAUNCH_SETUP_SEEN_REVISION`/`GPUI_HIGHLIGHTED_FEATURES_SEEN_REVISION` (main.rs) mirror `shared/first-launch-setup-settings.ts`; bump both together or GPUI replays/skips onboarding out of step with macOS. Re-entry: a `shared/` source-parity test (Batch 6.6 scanner-test precedent) if drift ever bites.
- [9.4] **First-run marker is marked before the modal actually presents** — GPUI persists the seen-revision then opens the tutorial video; a hypothetically missing modal-host bundle on true first run would consume the onboarding slot without showing the video (macOS marks after `openAppModal` returns). Broken-install edge only; the batch requirement was "relaunches never replay onboarding".

## Deferred from Batch 10

- [10.1a] **Titlebar update button has no hover percent label** — macOS shows a formatted download percent in the hover tooltip; GPUI renders the ring + disabled state only (GPUI titlebar buttons have no tooltip pattern yet). Re-entry: `render_titlebar_update_button` (main.rs).
- [10.1a] **Sparkle framework staging depends on the macOS build's SwiftPM artifacts** — `build-macos-app.sh` resolves Sparkle.framework from `build/*/SourcePackages/artifacts/sparkle` (or `GHOSTEX_GPUI_SPARKLE_FRAMEWORK`); a checkout that never built the macOS app packages a GPUI bundle without an updater unless the env var is set (`GHOSTEX_REQUIRE_SPARKLE=1` enforces presence on release builds). Re-entry: a pinned Sparkle download cache like `cef-cache` if standalone GPUI packaging is ever needed.
- [10.1a] **Sparkle version-drift guard refuses unknown driver shapes** — if a future bundled Sparkle drops the standard user-driver download callbacks the runtime subclass overrides, `GhostexGpuiSparkleUpdaterStart` reports -1 (honest warning toast) instead of shipping Sparkle's own download windows. Re-entry: rework `GpuiSparkleUpdater.m` overrides when Sparkle is upgraded past 2.x.
- [10.1c] **`--resume` does not rebuild a missing GPUI DMG** — resume reconstructs the GPUI Downloads row from existing release assets only; a release where the GPUI DMG upload failed is repaired with `--from publish-macos`. Re-entry: `resumeRelease` (release-ghostex.mjs).
- [10.1c] **GPUI DMG still embeds the Linux remote-gxserver packages** — the macOS app moved them (and bd) to on-demand GitHub assets with sealed checksums (CDXC:OnDemandAssets 2026-07-02); GPUI's remote-install path still reads `Contents/Resources/Web/gxserver-linux-*`, so its DMG is ~a quarter larger than it could be. Re-entry: port the on-demand asset manifest + download path into `gpui_install_bundled_remote_gxserver_and_read_token` (main.rs).
- [10.2] **open/edit file + line/column targeting into code-server not ported** — `ghostex://open|edit` and Finder file opens register + focus the file's project and wake Source, but the specific file/line/column is not opened: GPUI's Source runtime URL gate (`SourceCodeServerRuntimeTarget`) carries folder identity only, while macOS builds `?folder&file&line&column` URLs (`createCodeServerProjectEditorUrl`) + a code-server IPC file-open (`scheduleCodeServerFileOpen`). `ghostex edit --wait` token semantics are likewise not applicable yet. Re-entry: extend the Source target/URL gate with an explicit-navigation file field, or port the code-server IPC open.
- [10.2] **Quick terminal lands in a registered daemon project, launched via agentCommand** — macOS creates a per-invocation client-side Quick project row and TYPES `command\r` into a shell; GPUI registers/reuses the daemon project at the resolved cwd and creates the session with `createAgentSession launchSettings.agentCommand` (the Batch 1.4 `gx f` contract), so the shell does not remain after a one-shot command exits the way macOS's typed command does. Re-entry: `createOsIntegrationTerminal` (gxserver-runtime.ts) once a daemon quick-project/typed-input contract exists.
- [10.3] **`gpui.sidebar.focus` + `gpui.app.modal` scenarios remain definition-only** — the shared Settings scenario list has carried these GPUI entries (with `.jsonl` log files) since before Batch 10, but no writer exists for either; this batch's writers cover the macOS-parity trio + board + lifecycle + crash. Re-entry: `support_logs.rs` (add the log kinds) + the app-modal host / sidebar focus paths.
- [10.3] **Rust sanitizer is conservative, not a NativeLogPrivacy port** — `support_logs.rs` redacts path-like/long/control strings wholesale rather than reproducing the Swift sanitizer's field policy; the burden stays on callers to pass bounded ids/enums/counts. Re-entry: port `NativeLogPrivacy.sanitizePayload` if richer details are ever needed.
- [10.3] **Sidebar-refresh logging is bootstrap-funnel-scoped** — macOS samples per-presentation-delta/render events; GPUI logs the daemon-bootstrap toast funnel only (the bootstrap poll path keeps its explicit no-logs privacy contract). Re-entry: sampled presentation-delta events at the runtime boundary if repros need them.
- [10.4] **Edit menu omits Undo/Redo; Window menu omits Bring All To Front; no View/Help menus** — gpui routes Undo/Redo through app actions (not first-responder selectors), and GPUI has no undo authority for its native CEF/Ghostty views, so the items would be dead; the other macOS menus carry no GPUI-applicable items yet. Re-entry: `ghostex_gpui_main_menus` (main.rs) + CEF command routing for undo/redo.
- [10.4] **Window frame writes at quit/close only** — the bounds observer keeps the latest frame in process-local state and quit/last-window-close persist it (macOS also persists at willTerminate); a hard crash loses the last session's frame moves (the crash itself is captured in `gpui-crash-reports.log`). Re-entry: debounced writes in the bounds observer if it ever matters.
- [10.4] **Fullscreen/maximized restore trusts the stored kind** — `restored_gpui_window_bounds` re-applies Maximized/Fullscreen with the clamped restore bounds; macOS's exact zoomed-frame semantics (AD:3205-3260) may differ in edge multi-display layouts. Verify in the user pass.
