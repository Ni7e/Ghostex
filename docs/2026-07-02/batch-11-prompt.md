# Batch 11 prompt (T3 runtime ownership decision + Group 0 tail + deferred sweep)

This file IS the full prompt: an agent pointed at it should follow everything below as its instructions.

---

You are continuing the Ghostex → GPUI parity port. Set up context first, then implement Batch 11.

## Read these before doing anything

1. `docs/2026-07-02/gpui-parity-plan.md` — the verified roadmap. Batches 0 (foundations), 1 (Group 1), 2 (Group 2), 3 (Groups 3+4), 4 (Group 5), 5 (Group 6), 6 (Groups 7+8), 7 (Groups 9+10), 8 (Groups 11+12), 9 (Groups 13+14 remainder + Group 15), and 10 (Groups 16+17+18 + Group 0 shell) are implemented and building; their status blocks list deliberately deferred follow-ups, so do not "fix" those unless your batch includes them.
2. `docs/2026-07-02/reports/shell.md` + `docs/2026-07-02/reports/g13-14-phaseb.md` — gap evidence, now heavily stale. Known-stale on top of the Batch 0–9 staleness notes: shell §1 (daemon bootstrap), §4 (menu bar + update slot), §5 (quit/reopen), §9 (URL/file routing), §10 (crash/logging), and B.9/B.10/B.11 are ALL closed by Batches 0–10. What remains from shell.md is only §7's pane-divider clamps/reset 🔎, §6's workspace-bg-from-Ghostty-config note, §4's Resources-dropdown/editable-title partials, and §8's multi-pane restore eagerness. Every `main.rs` line anchor in the reports has drifted badly (~3MB file) — always `rg` for named symbols.
3. `docs/2026-07-02/deferred-out-of-scope.md` — the running deferred list, including the new "Deferred from Batch 10" section. Append to it whenever you defer something; never silently drop scope.

## Context you must not rediscover

- The app being ported lives in `gpui/` (Rust GPUI shell + CEF-hosted React surfaces). The hybrid architecture is final — no UI rewrites; parity = behavior completeness. GPUI reuses the SAME shared React sidebar + modal host via CEF; the GPUI titlebar itself is native Rust (the shared React titlebar-host serves only the Tips popover on GPUI).
- Already working (do not rebuild): the daemon bootstrap incl. never-stop-on-quit (`start_gpui_local_gxserver_bootstrap`); the CLI bridge server (58743, `gpui/src/cli_bridge.rs`); Sparkle auto-update end-to-end (`gpui/native/macos/GpuiSparkleUpdater.m` runtime-resolved framework + `start_gpui_sparkle_updater` + the native titlebar update button/ring — rg `render_titlebar_update_button`); DevID signing/notarization (`gpui/scripts/codesign-gpui-app.sh`; env-parameterized `GHOSTEX_GPUI_SIGN_IDENTITY`, `GHOSTEX_GPUI_NOTARIZE`, `GHOSTEX_NOTARY_PROFILE`; identity = the same Developer ID cert as the macOS app, user decision 2026-07-03); the release `--gpui` leg (`scripts/release-ghostex.mjs`, repo-root `appcast-gpui.xml`); runtime `ghostex://` + file-open routing (`receive_gpui_os_integration_urls` in main.rs + `handleGpuiOsIntegrationCommand` in `gpui/sidebar/gxserver-runtime.ts` — registered via gpui `Application::on_open_urls`, no ObjC shim needed); support logging + crash reports (`gpui/src/support_logs.rs`; panic hook installed on the first line of `fn main`); the native app menu bar (rg `ghostex_gpui_main_menus`); window-frame persistence (`state/gpui-window-frame-state.json`, rg `restored_gpui_window_bounds`); quit flush + quit-on-last-window-close (rg `flush_gpui_quit_persistence`).
- Reusable patterns (freshest examples): Rust→runtime script-injection bridges with pending queues (`gpui_os_integration_command_script` in main.rs + its `onOsIntegrationCommand` install in `gpui/sidebar/gxserver-runtime.ts`); runtime→Rust manifest `post*` fns (`gpui/src/cef/sidebar_bridge_manifest.rs` + `cef/macos.rs` + `cef/unsupported.rs` + a `receive_*` arm — `postProjectBoardConversationResponse` is the model); app-modal commands land in `handle_gpui_app_modal_sidebar_command` (main.rs; unmatched commands silently drop — check your message has an arm); GPUI state files persist under `ghostex_home_root().join("state/gpui-*.json")` (rg `gpui_window_frame_state_path` for the freshest example); startup tasks hook into the `view.update` block in `fn main` (rg `start_gpui_support_log_maintenance`); native FFI callback targets use the thread_local register/queue pattern (rg `register_gpui_os_integration_callback_target`).
- Support logging discipline: routine breadcrumbs go through `support_logs::append` with a scenario (shared-settings `diagnosticLogging.scenarios` ids shared with macOS; warning/error-like events always persist); callers pass only bounded ids/enums/counts — the writer redacts path-like/long strings. Do not add new log call sites outside a work item that includes them.
- A parallel workstream is landing GPUI-composited terminals (libghostty-vt; see `gpui/build.rs` `build_libghostty_vt`). Do not touch that build path; coordinate through the user if your items collide with terminal rendering files.
- IGNORE the stale trackers `gpui/SETTINGS_PARITY_PROGRESS.md`, `gpui/TITLEBAR_APP_MODAL_PARITY_PROGRESS.md`, `gpui/WORKSPACE_PARITY_PROGRESS.md`, and `docs/gpui-parity.md`.
- `gpui/src/main.rs` (~3MB), `gpui/sidebar/gxserver-runtime.ts` (~560KB), and `native/sidebar/native-sidebar.tsx` (~47k lines) — rg for symbols then read targeted ranges; never read whole files.

## Hard rules

- No CDXC comments. No tests in `gpui/` and none in the macOS app (repo policy — AGENTS.md; `shared/` and `gxserver-rs` tests are allowed).
- Never run `bun run start` or anything that restarts a running app unless the user asks.
- Verify with `cargo check` (plus one `cargo build` when you add ObjC shims or FFI so linking is proven) in `gpui/`; sidebar bundles build with `bunx vite build --config gpui/vite.config.ts` from the repo ROOT; if you touch `shared/`, `sidebar/`, or `native/sidebar/`, also run `bun run typecheck` and the touched `shared/` test files (repo root). `gpui/sidebar` is type-checked by neither; a targeted tsc there surfaces the ~11 pre-existing branded-type errors in untouched code — do not add new ones.
- Work one compile-clean commit-sized chunk at a time; after each item, ask the user to run the app and verify side-by-side with the macOS app before moving on.
- Follow AGENTS.md search routing (exclude `ghostty/**`, vendor trees, `node_modules/**`, `target/**`, builds).
- Never add fallbacks where correct behavior is possible; no hitTest/overlay hacks. Native OS-owned menus and native child windows ARE the accepted patterns for overlay surfaces. Honest stub toasts are the accepted pattern for affordances blocked on an open decision.

## Your job: Batch 11 — Batch 10 runtime verify → Decision #7 (T3) → Group 0 small tail → deferred sweep

Work these in order, one compile-clean commit-sized chunk each. Line anchors drift — rg the symbols.

0. **Batch 10 runtime verification carry-over (with the user, before new work).** Batch 10 landed code-verified only. Walk the plan's §16/§17/§18/Group-0 Batch 10 checks: a signed build (`GHOSTEX_GPUI_SIGN_IDENTITY="Developer ID Application: Mohamad Youssef (KTKP595G3B)" GHOSTEX_GPUI_NOTARIZE=1 bash gpui/scripts/build-macos-app.sh`) notarizes + staples clean and updates from `appcast-gpui.xml` on a second machine (or at minimum installs + runs clean); the titlebar update ring animates during a download; `ghostex://terminal|open|edit` and Finder Open-With reach the running app (incl. the `.command/.tool/.sh` Run/Edit/Cancel dialog); a forced panic writes `~/.ghostex/logs/gpui-crash-reports.log`; the app menu bar works incl. ⌘W close-pane, ⌘Q quit, ⌘H hide, ⌘M minimize; the window frame survives relaunch on a moved/second display; last-window-close quits and the quit flush persists state. Fix what breaks before new work; expect exactly one lid-sleep helper reinstall admin prompt on the ad-hoc→DevID transition (plan §14 Batch 9.3 notes).
1. **Decision #7 — T3 runtime ownership (L if resolved).** Ask the user ONCE at batch start: port macOS `NativeT3RuntimeLauncher` into GPUI, or move runtime ownership into gxserver-rs so both apps share one T3 authority over API? If resolved, THIS is the batch: implement the chosen ownership (macOS reference `native/macos/ghostexHost/Sources/ghostexHost/NativeT3CodePaneReproLog.swift:150-705,1316-1460` — port binding, ownership/health, process-tree teardown), then replace the honest stubs `killT3RuntimeServer`/`killT3RuntimeSession` (rg in main.rs), publish live `t3Server`/`t3Sessions` rows to the Running Sessions modal, add T3 hibernation, and the `t3BrowserAccess` cold-start path (deferred note [7.6] — rg `gpui_issue_t3_browser_access_link`). If the decision stays open, record that and skip to item 2.
2. **Group 0 small tail (M total; evidence: shell §7/§6/§4).** (a) Pane-divider min-size clamps + per-divider double-click reset parity (macOS TWV:14025/15379; GPUI split-tree drag handles — rg `split_drag` in main.rs; verify clamps, add double-click reset). (b) Derive the workspace background from the Ghostty config background color (macOS AD:2963; GPUI fixed `workspace_background_color` — rg it; read the config at Ghostty app-owner creation, no live reload — that contract is documented in deferred [5.4]). (c) Align the default sidebar width (macOS 235 vs GPUI `DEFAULT_SIDEBAR_WIDTH`). (d) Full Resources dropdown (macOS CPU/RAM sampling, Portless rows, restart controls, bulk quit/sleep — GPUI has the Running Sessions modal + a right-click batch menu, rg `show_gpui_resources_titlebar_menu`; macOS reference is the titlebar-host.tsx `resources` panel). (e) Editable titlebar session-title area (macOS `updateAppTitlebarTitle` AD:3603; GPUI passive label — rg `titlebar_project_label_from_latest_sidebar_snapshot`); (e) is optional — defer with a note if it turns L-sized.
3. **Deferred-tracker sweep (S/M).** Walk `docs/2026-07-02/deferred-out-of-scope.md` and close entries whose blockers landed in Batches 9–10. Known candidates: [3.5] floating-editor window frame persistence (frame infra now exists — rg `gpui_window_frame_state_path`); [3.5] app-lifecycle close-and-save for the floating editor (`flush_gpui_quit_persistence` is the hook); [2.5] fresh-install hotkeys seeding; the definition-only `gpui.sidebar.focus`/`gpui.app.modal` log scenarios ([10.3]) if you touch those flows anyway. Close only what genuinely fits; leave the rest untouched.
4. **Runtime checks (with the user).** Whatever items 1–3 landed, side-by-side with the macOS app, plus re-run any item-0 check that failed and was fixed.

## Decisions needed from the user

- Decision #7 (T3 runtime ownership) — ask ONCE at batch start; it decides whether item 1 exists in this batch.
- Decision #6 (git-editor + automate surfaces) stays open — ask only if the user brings it up.

## When you finish Batch 11

1. Update `docs/2026-07-02/gpui-parity-plan.md`: add a Batch 11 status block under the groups you touched (same style as the Batch 0–10 status blocks) and update the "Suggested execution order" line. Append deferred items to `docs/2026-07-02/deferred-out-of-scope.md`.
2. Write the exact prompt for the NEXT agent thread to implement Batch 12 into a NEW file, `docs/2026-07-02/batch-12-prompt.md` (also overwrite `docs/2026-07-02/next-agent-prompt.md` with a one-line pointer to it). Scope it from what actually remains: the unresolved decisions (#6, and #7 if still open), anything that slipped from Batch 11, and a final sweep of `docs/2026-07-02/deferred-out-of-scope.md` for entries whose blockers have landed. Anchor it on the plan + `docs/2026-07-02/reports/` evidence. That prompt must have this same structure: a header stating the file IS the full prompt, read-first list, context (including what must not be rebuilt), hard rules, ordered work items with file anchors, decisions needed, and — verbatim requirement — an instruction that the Batch 12 agent must in turn write `docs/2026-07-02/batch-13-prompt.md` the same way and hand the user a short send-prompt for it.
3. End your final message with ONLY a very short send-prompt for the user to copy, for example: `Read docs/2026-07-02/batch-12-prompt.md and follow it exactly — it is your full prompt for Batch 12 of the GPUI parity port.` Do NOT paste the full Batch 12 prompt inline.
